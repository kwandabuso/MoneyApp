<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MoneyApp.XamForms.Budget">

    <StackLayout>
        <Entry x:Name="Item" Placeholder="enter budget Item"/>
        <Entry x:Name="Amount" Placeholder="enter Amount"/>
        
        <Button x:Name="ButtonAddBudget" Text="Submit" Margin="20" Clicked="ButtonAddBudget_Clicked"/>

        <Label Text="TOTAL" x:Name="total"/>
        
        <ListView x:Name="MyListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text = "{Binding item}" TextColor = "DarkGreen"/>
                            <Label Text = "{Binding amount}" TextColor = "DarkGreen"/>

                        </StackLayout>
                    </ViewCell>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
       
    </StackLayout>
    
</ContentPage>