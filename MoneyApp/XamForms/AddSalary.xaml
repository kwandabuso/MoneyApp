<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MoneyApp.AddSalary">


    <StackLayout>
        <Entry x:Name="Salary" Placeholder="enter salary" Margin="20"/>
        <Entry x:Name="source" Placeholder="enter source" Margin="20"/>
        <Button x:Name="ButtonSalary" Text="Submit" Margin="20" Clicked="ButtonSalary_Clicked"/>
        
        <ListView x:Name="MyListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text = "{Binding mySource}" TextColor = "Black"/>
                            <Label Text = "{Binding mySalary}" TextColor = "Black"/>
                            <Label Text = "{Binding isActive}" TextColor = "Black"/>

                        </StackLayout>
                    </ViewCell>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>


</ContentPage>